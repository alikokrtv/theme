{%- comment -%}
  Nautica Today Section
  Nautica's vision and future
{%- endcomment -%}

{%- render 'section-spacing-collapsing' -%}

<style>
  .nautica-today {
    background: {{ section.settings.background_color | default: '#ffffff' }};
    padding: 6rem 2rem;
  }

  .nautica-today__container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .nautica-today__content {
    {%- if section.settings.reverse_layout -%}
      order: 2;
    {%- endif -%}
  }

  .nautica-today__image-wrapper {
    {%- if section.settings.reverse_layout -%}
      order: 1;
    {%- endif -%}
  }

  .nautica-today__heading {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    color: {{ section.settings.text_color | default: '#0b233f' }};
    margin-bottom: 2rem;
    letter-spacing: 0.02em;
  }

  .nautica-today__text {
    font-size: 1.15rem;
    line-height: 1.9;
    color: {{ section.settings.text_color | default: '#0b233f' }};
    opacity: 0.85;
  }

  .nautica-today__text p {
    margin-bottom: 1.5rem;
  }

  .nautica-today__text p:last-child {
    margin-bottom: 0;
  }

  .nautica-today__image {
    width: 100%;
    height: auto;
    border-radius: 12px;
    object-fit: cover;
  }

  @media (max-width: 999px) {
    .nautica-today__container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .nautica-today__content,
    .nautica-today__image-wrapper {
      order: unset;
    }
  }

  @media (max-width: 768px) {
    .nautica-today {
      padding: 4rem 1.5rem;
    }

    .nautica-today__text {
      font-size: 1rem;
    }
  }
</style>

<div {% render 'section-properties' %}>
  <div class="nautica-today">
    <div class="nautica-today__container">
      <div class="nautica-today__content">
        {%- if section.settings.heading != blank -%}
          <h2 class="nautica-today__heading">{{ section.settings.heading }}</h2>
        {%- endif -%}

        {%- if section.settings.content != blank -%}
          <div class="nautica-today__text">
            {{ section.settings.content }}
          </div>
        {%- endif -%}
      </div>

      {%- if section.settings.image != blank -%}
        <div class="nautica-today__image-wrapper">
          {%- capture sizes -%}(max-width: 999px) 100vw, 50vw{%- endcapture -%}
          {{- section.settings.image | image_url: width: section.settings.image.width | image_tag: class: 'nautica-today__image', sizes: sizes, widths: '400,600,800,1000,1200,1400,1600' -}}
        </div>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Nautica Today",
  "class": "shopify-section--nautica-today",
  "tag": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full width",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "reverse_layout",
      "label": "Reverse layout",
      "default": false,
      "info": "Image on left, text on right"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "NAUTICA TODAY"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>We have always been defined by our original vision of reimagined nautical style. Now, as we look towards the future of Nautica, we strive to be more reflective of the rapidly changing world around us, and connect with it in new ways: through sustainability, social responsibility, and smart functional design you want to live your life in.</p><p>As an authorized licensee, TechnoFashion proudly brings Nautica's premium audio equipment, mobile accessories, and smart lifestyle products to customers worldwide, maintaining the brand's commitment to quality and innovation.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#0b233f"
    }
  ],
  "presets": [
    {
      "name": "Nautica Today",
      "category": "Nautica"
    }
  ]
}
{% endschema %}

